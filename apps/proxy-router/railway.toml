[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/proxy-router/Dockerfile"
buildContext = "apps/proxy-router"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[deploy.envVars]]
name = "PORT"
value = "3002"
