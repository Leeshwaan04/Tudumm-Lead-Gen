[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/storage-service/Dockerfile"
buildContext = "apps/storage-service"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[deploy.envVars]]
name = "PORT"
value = "8080"
