[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api-gateway/Dockerfile"
buildContext = "apps/api-gateway"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[deploy.envVars]]
name = "PORT"
value = "8080"
