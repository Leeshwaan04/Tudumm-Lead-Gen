[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/scheduling-service/Dockerfile"
buildContext = "apps/scheduling-service"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[deploy.envVars]]
name = "PORT"
value = "8080"
