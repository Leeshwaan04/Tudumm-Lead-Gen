[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/orchestrator/Dockerfile"
buildContext = "apps/orchestrator"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[deploy.envVars]]
name = "PORT"
value = "8080"
