[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/enrichment-service/Dockerfile"
buildContext = "apps/enrichment-service"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[deploy.envVars]]
name = "PORT"
value = "8000"
