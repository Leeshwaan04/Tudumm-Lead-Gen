[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/auth-service/Dockerfile"
buildContext = "apps/auth-service"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[deploy.envVars]]
name = "PORT"
value = "8080"
