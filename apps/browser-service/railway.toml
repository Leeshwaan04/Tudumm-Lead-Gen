[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/browser-service/Dockerfile"
buildContext = "apps/browser-service"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[deploy.envVars]]
name = "PORT"
value = "3001"
