{
  "name": "@tudumm/infra",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc",
    "deploy": "cdk deploy --all --require-approval never",
    "diff": "cdk diff",
    "synth": "cdk synth",
    "bootstrap": "cdk bootstrap"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "aws-cdk": "^2.170.0",
    "typescript": "^5.7.2",
    "tsx": "^4.19.2",
    "@types/node": "^22.0.0"
  }
}
